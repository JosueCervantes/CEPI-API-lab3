{
  "devDependencies": {
    "sass": "^1.85.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "json-server": "^0.17.4",
    "multer": "^1.4.5-lts.2"
  },
  "scripts": {
    "build-sass": "sass xd.scss style.css",
    "server": "json-server --watch db.json --port 3000"
  }
}
