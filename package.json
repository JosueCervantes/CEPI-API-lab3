{
  "devDependencies": {
    "json-server": "^1.0.0-beta.3",
    "sass": "^1.85.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "scripts": {
    "build-sass": "sass xd.scss style.css",
    "server": "json-server --watch db.json --port 3000"
  }
}
